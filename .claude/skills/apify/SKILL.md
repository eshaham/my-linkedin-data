---
name: apify
description: Use the apify CLI to manage Apify actors, runs, and datasets.
---

# Apify

CLI for the [Apify](https://apify.com) platform — running actors, fetching dataset items, managing schedules. Run `apify --help` for available commands.

API token is available as `APIFY_TOKEN` environment variable.

See: https://github.com/apify/apify-cli
